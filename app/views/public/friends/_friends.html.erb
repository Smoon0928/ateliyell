<div id="follow_form">
    <% if current_user.following?(user) %>
      <%= link_to "フォロー中", unfollow_path(user.id), class: 'btn btn-outline-primary', method: :delete, remote: true %>
    <% else %>
      <%= link_to "フォローする", follow_path(user.id), class: 'btn btn-outline-primary', method: :post, remote: true %>
    <% end %>
</div>

